package com.example.service;import com.example.mywifihelper.ClientActivity;import com.example.mywifihelper.MainViewActivity;import com.example.mywifihelper.ServiceActivity;import com.example.util.AppManager;import android.content.BroadcastReceiver;import android.content.Context;import android.content.Intent;import android.os.Bundle;import android.util.Log;public class ListenerSocketReceiver extends BroadcastReceiver{	@Override	public void onReceive(final Context context, Intent intent) {		Bundle bundle = intent.getBundleExtra("bundle");		Log.i("MYMYMY","到了这个网络判断 a");		if (bundle.getInt("flag") == 0 && bundle != null) { // Socket连接断开			// 第一次启动为true			//SocketNetService.isFristConn = true;			// 循环使其连接			new Thread(new Runnable() {				@Override				public void run() {					try {						Thread.sleep(3500); // 休眠5秒						Intent serviceIntent = new Intent();						// 开启服务						if(!AppManager.getAppManager().hasActivity(ServiceActivity.class)){						serviceIntent.setClass(context, ClientNetService.class);						serviceIntent.setAction("sljppff.service.ClientNetService");						context.startService(serviceIntent);}					} catch (InterruptedException e) {						e.printStackTrace();					}				}			}).start();		}	}}